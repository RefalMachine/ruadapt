# FP8_REVIEW.md — ревизия fp8-реализации: что устарело, что верно, где скорость

Дата: 2026-08-14. Предмет: `ruadapt/training/core/fp8.py`, fp8-ветки в
`core/model.py` / `train.py`, конфиги `sft_v9_fp8*`, и достоверность
[FP8.md](FP8.md), `trash/FP8_CHECK.md`, `trash/ANALYSYS.md` §8.

> **Статус документа: замеры и выводы актуальны; правки по §6 выполнены
> 2026-08-14.** Документ сохранён как обоснование (методика и числа), а
> действующие артефакты после уборки таковы:
> - **[PERF_PLAN.md](PERF_PLAN.md)** — канонический план ускорения (§5 этого
>   файла перенесён туда и расширен), плюс опорные факты по железу/памяти/fill;
> - **[FP8.md](FP8.md)** — реализация fp8: §6 закрыт полным прогоном, §7
>   описывает фактический merge, §8 сведён к ссылке на PERF_PLAN;
> - `trash/ANALYSYS.md`, `trash/FP8_CHECK.md` — архив (журнал v7→v8 и закрытый
>   аудит), ссылки ниже указывают на них по старым именам.

Окружение проверки: torch **2.11.0+cu128**, transformers **5.9.0**, peft
**0.19.1**, H100 NVL (SM 9.0) ×8. Все замеры этого документа — GPU 4
(свободная), пока на GPU 0–3 шёл продовый прогон.

---

## 1. Суть вопроса (в одном абзаце)

База при LoRA-SFT полностью заморожена ⇒ её веса — константы forward'а. Их
держат в fp8 (официальный чекпоинт `Qwen/Qwen3.5-27B-FP8`, E4M3, блок
128×128), а считают в bf16, де-квантизируя вес внутри forward. Освободившиеся
~30 GiB/карту позволяют **выключить gradient checkpointing** и не платить
~31% за рекомпутацию. Нативный путь HF для этого не годится: у
`FineGrainedFP8HfQuantizer` `is_trainable=False`, а forward `FP8Linear` уходит
в Triton/DeepGEMM без autograd. Отсюда дизайн: собственный
`autograd.Function` с `dequant → F.linear`, backward ре-деквантизирует вес и
не сохраняет ничего.

**Вердикт по дизайну: он верен и подтверждён продовым прогоном.** Все
блокирующие факты (§3 FP8.md) я перепроверил — они на месте.

---

## 2. Главная новость: полный прогон v9 завершён (в документации не отражён)

`/workdir/data/models/Qwen3.5-27B-FP8_with_help_r128_a128_nlh_lr1e-4_e3_bs256_packed_4gpu_v9fp8/checkpoint-4908/trainer_state.json`:

| Метрика | Значение |
|---|---|
| Шагов | 4908 (3 эпохи), `train_runtime` 26 207 с (7.3 ч) |
| non-padding ток/с (4 GPU) | **8753** в окне 20→60, **8752** в окне 100→1000, 8734 за весь прогон |
| loss | 0.806 → 0.150 |
| eval_loss по шагам | 500: 0.5461 · 1000: 0.5248 · **1500: 0.5171 (мин)** · 2000: 0.5322 · 2500: 0.5241 · 3000: 0.5189 · 3500: 0.5821 · 4000: 0.5882 · 4500: 0.5870 · 4908: 0.5876 |

Два вывода, которых нет ни в одном md:

1. **Заявленная скорость подтверждена на полном прогоне и стабильна**: 8753
   ток/с в разгонном окне и 8752 на длинном окне 100→1000 — деградации по
   ходу 7-часового прогона нет (важно: значит torch.compile-кэш dequant не
   рекомпилируется и фрагментация аллокатора не накапливается). FP8.md §9
   обещает 8856 — совпадение в пределах 1%.
2. **Качество: fp8 не мешает, но 3 эпохи — это переобучение.** Минимум
   eval_loss на шаге 1500 (≈1 эпоха), затем рост на +0.07 и плато. FP8.md §6
   («нужен полный прогон + eval») **закрыт**, но вывод из него —
   не про fp8, а про расписание: оптимум ≈1–1.5 эпохи, и `save_steps=500` при
   `save_total_limit=2` сохраняет только хвост, т.е. **лучший чекпоинт
   физически не сохранён**. Это следует поправить (`load_best_model_at_end` +
   `metric_for_best_model=eval_loss`) прежде, чем гнать новые прогоны.

---

## 3. Что устарело / неверно в существующих документах

### 3.1. Стоимость dequant завышена в 6–11 раз (ANALYSYS §8.4 п.4, §8.6/Н11, FP8_CHECK Д11)

Документы утверждают «~280 мс/проход dequant», и на этом строится вся логика
§8 FP8.md («dequant дорог ⇒ следующий рычаг W8A8»). Замер по фактической
геометрии всех 400 квантованных слоёв 27B:

| Режим | Суммарный dequant за один проход | Эфф. полоса |
|---|---|---|
| eager (`dequantize_fp8_block`) | **155 мс** | 400–435 GB/s |
| `torch.compile` (продовый режим) | **25 мс** | 1500–3300 GB/s |

Продовая конфигурация — компилированная, значит реальная цена **25 мс/проход,
50 мс за микробатч** (fwd+bwd). При измеренном шаге 5.34 с / 4 микробатча =
1.33 с на микробатч это **≈3.7% времени**.

Иронично: априорная оценка «~40 мс (~3%)», которую FP8_CHECK.md Д11 объявил
расхождением «в 7 раз», **оказалась точной**. Ошибочна была не она, а
величина 280 мс — она получена вычитанием из сквозного времени шага
(fp8-ckptON против bf16-синтетики), т.е. в неё попал не dequant, а всё
различие двух прогонов. Метка «априорная оценка неверна» в трёх файлах должна
быть снята.

**Почему это принципиально.** Из «dequant стоит 280 мс» следует, что его
устранение (W8A8) даёт десятки процентов. Из «dequant стоит 50 мс» следует,
что **устранение dequant даёт максимум ~4%**. Мотивировка пункта 1 в FP8.md §8
неверна — но сам пункт остаётся ценным по другой причине (§4.1).

### 3.2. FP8.md §7 (merge) — процедура описана, но применялась другая

§7 предписывает грузить базу через `FineGrainedFP8Config(dequantize=True)` и
мультимодальный класс. Фактически merge сделан иначе (и это сработало):
`base_model_name_or_path` в `adapter_config.json` подменён с
`Qwen/Qwen3.5-27B-FP8` на `Qwen/Qwen3.5-27B`, после чего обычный
`scripts/merge_lora.py` смержил адаптер в **bf16-базу**. Проверено:
`checkpoint-4908/adapter_config.json` содержит FP8-базу, а корневой
`adapter_config.json` — bf16; merged-модель на диске — bf16
`Qwen3_5ForConditionalGeneration`, 53 GB, без `quantization_config`.

Это проще, чем §7, и корректно по ключам, но содержит **непроверенное
допущение о качестве**, которое нужно зафиксировать явно: адаптер обучался
компенсировать forward *квантованной* базы, а мержится в *неквантованную*.
Разница баз — это и есть собственная ошибка fp8 (~3–6% на вес). Ожидание, что
это безвредно, разумное (смещение мало и в основном не выучено адаптером), но
**не измерено**. Дешёвая проверка — §4.4.

Также §7 в шапке помечает merge как «не реализован» — устарело: он выполнен и
результаты (`_merged`, `_apc1.5/2.0/2.5_merged`) лежат в `/workdir/data/models/`.

### 3.3. AGENTS.md вообще не знает про fp8 и про 27B

`AGENTS.md` описывает целевую модель как Qwen3.5-**2B** (291K vocab, 25 слоёв,
dim 2048) и не упоминает ни `fp8_storage`, ни packing, ни v8/v9. Вся работа
последних недель — 27B (64 слоя, dim 5120, гибридное внимание). Раздел «Current
Goal» («миграция завершена, написать тесты») тоже отстал. Это главный источник
дезориентации для нового человека/агента в проекте.

### 3.4. Мелкое

- FP8.md §5.2 и ANALYSYS §8.4 корректно помечены как исторические/оспоренные —
  претензий нет, но при следующей чистке их лучше свести в один раздел, т.к.
  сейчас три файла описывают одни и те же прогоны с разными вердиктами.
- FP8_CHECK.md Д6 (пропатченный модуль не пиклится) остаётся открытым и не
  закрыт ни фиксом, ни явным «принято». Сегодня безвреден (сохраняются только
  адаптеры), но `torch.save(model)` упадёт.

---

## 4. Корректность и оптимальность реализации

### Корректность — замечаний по существу нет

Перечитал `fp8.py` целиком; фиксы аудита на месте и адекватны:
`save_for_backward` убран (тест на `saved_tensors == ()`), `to(dtype, copy=True)`
закрывает алиасинг `mul_`, `assert bias is None` + fail-fast в
`patch_fp8_linear_for_training`, `quantize_fp8_block` удалён, фикстуры тестов
переведены на HF `Fp8Quantize`. `verify_fp8_storage` вызывается дважды (до и
после PEFT) и ловит именно те два класса отказа, которые убивают backward
молча. Математика backward верна: `grad_input = grad_output @ weight` для
замороженного веса, `grad_weight` не нужен.

Две вещи, которые стоит знать (не баги, но не задокументированы):

1. **`_ACTIVE_DEQUANT` — глобальное состояние модуля.** `set_dequant_compiled`
   переключает модульную глобаль, а `_make_dequant_forward` читает её через
   замыкание в момент вызова. Т.е. флаг действует на все модели в процессе, и
   порядок «патч → set_compiled» неважен. Работает, но это скрытая связь;
   в тестах она уже проявляется (тест паритета обязан сбрасывать флаг).
2. **`dequantize_fp8_block` не умеет 3D-веса экспертов.** Для текущей модели
   неактуально (`FP8Experts` идёт своим путём), но если появится MoE-чекпоинт
   с квантованными экспертами, `_is_fp8_quantized_linear` их не поймает, и
   `verify_fp8_storage` **не сработает как страховка** — он проверяет только
   `FP8Linear`. Backward тогда умрёт в Triton. Стоит расширить verify на
   `FP8Experts`.

### Оптимальность — где реально уходит время

Измерения при M=3072 (продовый чанк), суммарно по всем 400 квантованным слоям
за один forward-проход:

| Что | Время | TFLOPS |
|---|---|---|
| bf16 `F.linear` (эталон) | 272 мс | 493–519 |
| компилированный `dequant + F.linear` | 292 мс | 458–485 |
| **накладные расходы fp8-пути** | **+7.0%** | — |

Разбиение шага (4 GPU, чанк 3072, accum 4, fill 0.9491, 5.34 с/шаг ⇒ 1.33 с на
микробатч):

- GEMM квантованных слоёв ≈ 272 мс fwd + ~2× в bwd ≈ **0.8 с ≈ 60% микробатча**;
- dequant ≈ 50 мс ≈ **3.7%**;
- остальное (FLA-ядра, нормы, эмбеддинги/lce, DDP, Python) ≈ 36%.

**Вывод: реализация близка к оптимуму для выбранного режима.** Из fp8-пути
как такового выжимать больше почти нечего: полный отказ от dequant вернул бы
максимум ~4%. Основной массив времени — сами bf16-GEMM'ы, и они уже идут на
~500 TFLOPS, т.е. у compute-потолка bf16 на гибридной архитектуре (совпадает с
ANALYSYS §3.11: 270–300 TFLOPS в единицах 6N).

---

## 5. План по ускорению

> ⚠️ **Перенесено.** Канонический (поддерживаемый) план — в
> [PERF_PLAN.md](PERF_PLAN.md) §3; ниже — исходная формулировка с обоснованием
> замеров.

Отсортировано по отношению «выигрыш / риск». Пункты 1–2 дешёвые, 3 — главный,
4 — гигиена качества.

### 5.1. `ddp_find_unused_parameters: false` — бесплатные проценты

**Факт по коду** (`transformers/trainer.py`, `_build_accelerator_args`): при
`ddp_find_unused_parameters=None` значение берётся как
`not (gradient_checkpointing)` **только для `PreTrainedModel`**; PEFT-модель
таковой не является ⇒ ветка `else: find_unused = True`. В v9 ckpt выключен, и
модель обёрнута PEFT — значит DDP на каждом шаге делает лишний обход графа
(автопоиск неиспользуемых параметров) при том, что обучаемых тензоров всего
512 и все они используются всегда.

Действие: добавить `"ddp_find_unused_parameters": false` в
`configs/sft_v9_fp8_*gpu.json`. Ожидание: 1–3%. Риск: нулевой при
неизменной топологии графа (если появятся условно-неиспользуемые модули, DDP
упадёт явной ошибкой, а не тихо).

### 5.2. Селективный gradient checkpointing ради чанка 4096

Плато по длине чанка (ANALYSYS §3.12) даёт всего +1.8% ток/с, но **fill**
растёт заметно: 0.9491 (3072) → 0.9599 (4096) = **+1.1% полезных токенов**, и
это постоянный множитель. Сейчас 4096 не влезает: 89.3 GiB при 3072, оценка
~103 GiB при 4096.

Идея: чекпоинтить только 48 слоёв GatedDeltaNet (именно они держат ~38 GiB
активаций), оставив 16 full-attention слоёв без рекомпутации. Стоимость
рекомпутации падает пропорционально доле, память — тоже.

Ожидание: +1–3% суммарно (fill + плато) минус цена частичной рекомпутации —
может выйти в ноль. **Замерять до внедрения**, приоритет низкий.

### 5.3. W8A8 FP8-compute — главный рычаг, но заблокирован версией cuBLAS

Здесь лежит единственный крупный резерв: fp8-тензорные ядра быстрее bf16 в
~1.7–1.9× на этих формах. Измерено (M=3072, N=17408, K=5120):

| Путь | Время | TFLOPS |
|---|---|---|
| bf16 `F.linear` | 1.081 мс | 507 |
| `F.scaled_mm`, TensorWise | 0.642 мс | **853** |
| `F.scaled_mm`, TensorWise, `use_fast_accum=True` | 0.573 мс | **956** |
| `F.scaled_mm`, BlockWise1x128 × BlockWise128x128 | — | **NotImplementedError** |

**Блокер (проверено запуском):** нативный «DeepSeek-style» рецепт
`(1x128, 128x128)` — ровно формат нашего чекпоинта — требует
**cublasLt ≥ 12.9**, а стек собран на cu128 (12.8):

```
NotImplementedError: DeepSeek style (1x128, 128x128) scaling requires cublasLt >= 12.9
```

Т.е. пункт 1 FP8.md §8 («dgrad через `torch._scaled_mm` прямо над
хранящимися весами») **на текущем стеке нереализуем в блочном варианте**.
`torch._C._ScalingType` подтверждает, что API для `BlockWise128x128` есть —
не хватает только рантайма cuBLAS.

Оценка потолка, если разблокировать: GEMM ≈ 60% микробатча, ускорение GEMM
1.7× ⇒ экономия ≈ 0.6·(1−1/1.7) ≈ **25% времени шага ⇒ +33% ток/с**. Это и
есть недостающая часть до верхней границы гипотезы Г6 (+20–50%).

Три пути, в порядке предпочтения:

1. **Обновить torch/CUDA до cublasLt ≥ 12.9** (torch на cu129/cu130) и
   повторить замер блочного рецепта. Самое дешёвое по коду и единственное,
   что сохраняет точность чекпоинта как есть. Требует проверки совместимости
   FA3 / flash-linear-attention / liger / vLLM 0.21 — это основной риск.
2. **Активации в fp8 + per-tensor или rowwise скейлы весов.** Работает уже
   сейчас (853–956 TFLOPS), но: (а) требует ре-квантизации весов из блочного
   формата в per-tensor/rowwise, что **ухудшает точность** относительно
   вендорского чекпоинта; (б) квантование активаций — это уже настоящее
   W8A8-обучение, градиентный сигнал грязнее (сейчас активации честный bf16 —
   аргумент §1 FP8.md); (в) rowwise на этом torch требует `scale_b.size(0)==1`,
   т.е. поддержан не в той раскладке, которая нужна. Делать только если 1
   невозможен, и обязательно с параллельным eval.
3. **Только dgrad в fp8, forward оставить bf16.** Половина выигрыша при
   вдвое меньшем риске по качеству (веса и так fp8, `grad_output`
   квантуется в fp8 динамически). Разумный компромисс, если 1 недоступен.

Порядок работ: сначала микро-бенч отдельным скриптом (`tests/perf/`), только
затем интеграция в `_DequantLinearFunction`. Не начинать интеграцию до
подтверждённого выигрыша на bench'е.

### 5.4. Дешёвая проверка допущения о merge (качество, не скорость)

Замерить расхождение выходов: адаптер над fp8-базой vs тот же адаптер над
bf16-базой (`merged`-модель), на 100–200 промптах из val. Метрика — KL/PPL и
доля расхождений в argmax. Инфраструктура есть: `tests/training/test_fp8_parity.py`
(gate `RUN_FP8_PARITY=1`) уже делает почти это, но сравнивает fp8-storage с
`dequantize=True` — нужен вариант «адаптер поверх двух разных баз».

Заодно закрывает вопрос, законен ли применённый merge-путь (§3.2), и это
единственное место, где fp8 действительно может стоить качества.

### 5.5. Не рычаг (проверено, закрывать)

- **Устранение dequant** — потолок ~4% (§3.1), не стоит усилий сам по себе.
- **eager → compile для dequant** — уже включено (`fp8_compile_dequant: true`),
  даёт 6× на самой операции; ничего не осталось.
- **Данные** — dataloader-воркеры idle (ANALYSYS §3.11 п.6), `main_process_first`
  снял дублирование подготовки; узкого места нет.

---

## 6. Что поправить в документации — ВЫПОЛНЕНО 2026-08-14

| Файл | Правка | Статус |
|---|---|---|
| `AGENTS.md` | Добавить 27B/v8/v9/fp8 в «Key Technical Details», обновить «Current Goal»; целевая модель была описана как 2B (§3.3) | ✅ переписаны State / Key Technical Details / Current Goal / References / глоссарий |
| `FP8.md` §8 п.1 | Переписать мотивацию: dequant стоит ~4%, а не 30%; выигрыш W8A8 — от ускорения GEMM. Добавить блокер cublasLt ≥ 12.9 | ✅ §8 сведён к ссылке на [PERF_PLAN.md](PERF_PLAN.md) §3.3, мотивация исправлена |
| `FP8.md` §6 | Закрыть: полный прогон есть, eval-кривая в §2 этого файла; отметить переобучение на 3 эпохах | ✅ §6 переписан по `trainer_state.json` |
| `FP8.md` §7 | Снять «не реализован»; описать фактический путь merge (подмена базы на bf16) и оговорку о качестве | ✅ §7.1 (описанный путь) / §7.2 (применённый) + оговорка |
| `ANALYSYS.md` §8.6/Н11, `FP8_CHECK.md` §6/Д11 | Снять метку «априорная оценка ~40 мс неверна» — она верна; неверна цифра 280 мс | ✅ оба файла перенесены в `trash/`, ошибки перечислены в их шапках |
| `FP8_CHECK.md` Д6 | Либо фикс (модульный callable вместо замыкания), либо явное «принято, не лечим» | ✅ «принято, не лечим» — [FP8.md](FP8.md) §4.1, [PERF_PLAN.md](PERF_PLAN.md) §5.1 |

Пункты §5 (план ускорения) перенесены в [PERF_PLAN.md](PERF_PLAN.md) и там же
поддерживаются; в конфигах обучения ничего не менялось.

---

## 7. Воспроизведение замеров этого файла

```bash
# dequant: eager vs compiled, по реальной геометрии 27B (GPU, ~2 мин)
#   shapes: mlp.gate/up 17408x5120 x128, mlp.down 5120x17408 x64,
#           attn q/k/v/o x16..32, linear_attn in_qkv/in_z/out x48
#   меряется ruadapt.training.core.fp8._ACTIVE_DEQUANT при
#   set_dequant_compiled(False/True)

# overhead fp8-пути: F.linear(bf16) vs F.linear(compiled_dequant(...)) при M=3072

# W8A8: блокер рецепта
CUDA_VISIBLE_DEVICES=4 python - <<'PY'
import torch
from torch.nn.functional import scaled_mm
from torch._C import _ScalingType as ST, _SwizzleType as SW
M,N,K=3072,17408,5120; dev='cuda'
xq=torch.randn(M,K,device=dev).to(torch.float8_e4m3fn)
w=torch.randn(N,K,device=dev).to(torch.float8_e4m3fn)
sx=torch.rand(M,K//128,device=dev); sw=torch.rand(N//128,K//128,device=dev)
scaled_mm(xq,w.t(),sx,ST.BlockWise1x128,sw,ST.BlockWise128x128,
          SW.NO_SWIZZLE,SW.NO_SWIZZLE,None,torch.bfloat16,None,False)
# -> NotImplementedError: ... requires cublasLt >= 12.9
PY

# полный прогон v9: скорость и eval
python - <<'PY'
import json
p='/workdir/data/models/Qwen3.5-27B-FP8_with_help_r128_a128_nlh_lr1e-4_e3_bs256_packed_4gpu_v9fp8/checkpoint-4908/trainer_state.json'
st=json.load(open(p)); log=[l for l in st['log_history'] if 'loss' in l]
r=lambda a,b:(b['num_input_tokens_seen']-a['num_input_tokens_seen'])/(b['train_runtime']-a['train_runtime'])
print('20->60', r(log[19],log[59]), '100->1000', r(log[99],log[999]))
print([(l['step'],round(l['eval_loss'],4)) for l in st['log_history'] if 'eval_loss' in l])
PY
```
